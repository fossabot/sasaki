[package]
name = "sasaki"
version = "0.0.8"
authors = [ "Cynede <codecaves@gmail.com>" ]
repository = "https://github.com/Cynede/sasaki"
readme = "README.md"
keywords = [ "discord", "bot" ]

[dependencies]
rand = "*"
chrono = "*"
typemap = "*"
rust-ini = "*"
argparse = "*"
log = "*"
env_logger = "*"
curl = "*"
regex = "*"
serenity = { version = "*", features = ["cache", "framework", "standard_framework", "voice"] }

# I don't need bd for now
#diesel = { version = "*", features = ["sqlite", "chrono"] }
